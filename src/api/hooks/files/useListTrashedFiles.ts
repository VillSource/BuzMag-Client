/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { ListTrashedFilesOptions, ListTrashedFilesStatus200 } from '../../types/ListTrashedFiles'
import { queryOptions } from '@tanstack/react-query'
import { listTrashedFiles } from '../../clients/listTrashedFiles'

export const listTrashedFilesQueryKey = ({ query }: Omit<ListTrashedFilesOptions, 'headers'> = {}) => [{ url: '/api/v1/files/trash' }, ...(query ? [query] : [])] as const

type ListTrashedFilesQueryKey = ReturnType<typeof listTrashedFilesQueryKey>

export function listTrashedFilesQueryOptions({ query }: ListTrashedFilesOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listTrashedFilesQueryKey({ query })
  return queryOptions<ListTrashedFilesStatus200, ResponseErrorConfig<Error>, ListTrashedFilesStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await listTrashedFiles({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
