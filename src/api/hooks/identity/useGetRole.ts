/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { GetRoleOptions, GetRoleStatus200, GetRoleStatus401, GetRoleStatus403, GetRoleStatus404 } from '../../types/GetRole'
import { queryOptions } from '@tanstack/react-query'
import { getRole } from '../../clients/getRole'

export const getRoleQueryKey = ({ path }: Omit<GetRoleOptions, 'headers'>) => [{ url: '/api/v1/identity/roles/:id', params: path }] as const

type GetRoleQueryKey = ReturnType<typeof getRoleQueryKey>

export function getRoleQueryOptions({ path }: GetRoleOptions, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = getRoleQueryKey({ path })
  return queryOptions<GetRoleStatus200, ResponseErrorConfig<GetRoleStatus401 | GetRoleStatus403 | GetRoleStatus404>, GetRoleStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await getRole({ ...config, path, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
