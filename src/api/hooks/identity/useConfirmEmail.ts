/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { ConfirmEmailOptions, ConfirmEmailStatus200 } from '../../types/ConfirmEmail'
import { queryOptions } from '@tanstack/react-query'
import { confirmEmail } from '../../clients/confirmEmail'

export const confirmEmailQueryKey = ({ query }: Omit<ConfirmEmailOptions, 'headers'>) => [{ url: '/api/v1/identity/confirm-email' }, ...(query ? [query] : [])] as const

type ConfirmEmailQueryKey = ReturnType<typeof confirmEmailQueryKey>

export function confirmEmailQueryOptions({ query }: ConfirmEmailOptions, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = confirmEmailQueryKey({ query })
  return queryOptions<ConfirmEmailStatus200, ResponseErrorConfig<Error>, ConfirmEmailStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await confirmEmail({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
