/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { SearchTicketsOptions, SearchTicketsStatus200 } from '../types/SearchTickets'
import { queryOptions } from '@tanstack/react-query'
import { searchTickets } from '../clients/searchTickets'

export const searchTicketsQueryKey = ({ query }: Omit<SearchTicketsOptions, 'headers'> = {}) => [{ url: '/api/v1/tickets' }, ...(query ? [query] : [])] as const

type SearchTicketsQueryKey = ReturnType<typeof searchTicketsQueryKey>

export function searchTicketsQueryOptions({ query }: SearchTicketsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = searchTicketsQueryKey({ query })
  return queryOptions<SearchTicketsStatus200, ResponseErrorConfig<Error>, SearchTicketsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await searchTickets({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
