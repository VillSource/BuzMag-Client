/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { ListTrashedTicketsOptions, ListTrashedTicketsStatus200 } from '../../types/ListTrashedTickets'
import { queryOptions } from '@tanstack/react-query'
import { listTrashedTickets } from '../../clients/listTrashedTickets'

export const listTrashedTicketsQueryKey = ({ query }: Omit<ListTrashedTicketsOptions, 'headers'> = {}) => [{ url: '/api/v1/tickets/trash' }, ...(query ? [query] : [])] as const

type ListTrashedTicketsQueryKey = ReturnType<typeof listTrashedTicketsQueryKey>

export function listTrashedTicketsQueryOptions({ query }: ListTrashedTicketsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listTrashedTicketsQueryKey({ query })
  return queryOptions<ListTrashedTicketsStatus200, ResponseErrorConfig<Error>, ListTrashedTicketsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await listTrashedTickets({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
