/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { ChangeTenantActivationOptions, ChangeTenantActivationStatus200, ChangeTenantActivationStatus400, ChangeTenantActivationStatus401, ChangeTenantActivationStatus403, ChangeTenantActivationStatus404 } from '../../types/ChangeTenantActivation'
import { mutationOptions } from '@tanstack/react-query'
import { changeTenantActivation } from '../../clients/changeTenantActivation'

export const changeTenantActivationMutationKey = () => [{ url: '/api/v1/tenants/:id/activation' }] as const

export function changeTenantActivationMutationOptions<TContext = unknown>(config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const mutationKey = changeTenantActivationMutationKey()
  return mutationOptions<ChangeTenantActivationStatus200, ResponseErrorConfig<ChangeTenantActivationStatus400 | ChangeTenantActivationStatus401 | ChangeTenantActivationStatus403 | ChangeTenantActivationStatus404>, ChangeTenantActivationOptions, TContext>({
    mutationKey,
    mutationFn: async({ path, body }) => {
      const { data } = await changeTenantActivation({ ...config, path, body, throwOnError: true })
      return data
    },
  })
}
