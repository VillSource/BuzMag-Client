/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { DiscoverChannelsOptions, DiscoverChannelsStatus200 } from '../types/DiscoverChannels'
import { queryOptions } from '@tanstack/react-query'
import { discoverChannels } from '../clients/discoverChannels'

export const discoverChannelsQueryKey = ({ query }: Omit<DiscoverChannelsOptions, 'headers'> = {}) => [{ url: '/api/v1/chat/channels/discover' }, ...(query ? [query] : [])] as const

type DiscoverChannelsQueryKey = ReturnType<typeof discoverChannelsQueryKey>

export function discoverChannelsQueryOptions({ query }: DiscoverChannelsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = discoverChannelsQueryKey({ query })
  return queryOptions<DiscoverChannelsStatus200, ResponseErrorConfig<Error>, DiscoverChannelsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await discoverChannels({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
