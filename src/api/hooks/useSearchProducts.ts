/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { SearchProductsOptions, SearchProductsStatus200 } from '../types/SearchProducts'
import { queryOptions } from '@tanstack/react-query'
import { searchProducts } from '../clients/searchProducts'

export const searchProductsQueryKey = ({ query }: Omit<SearchProductsOptions, 'headers'>) => [{ url: '/api/v1/catalog/products' }, ...(query ? [query] : [])] as const

type SearchProductsQueryKey = ReturnType<typeof searchProductsQueryKey>

export function searchProductsQueryOptions({ query }: SearchProductsOptions, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = searchProductsQueryKey({ query })
  return queryOptions<SearchProductsStatus200, ResponseErrorConfig<Error>, SearchProductsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await searchProducts({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
