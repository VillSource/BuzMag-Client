/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { GetWebhookDeliveriesOptions, GetWebhookDeliveriesStatus200 } from '../types/GetWebhookDeliveries'
import { queryOptions } from '@tanstack/react-query'
import { getWebhookDeliveries } from '../clients/getWebhookDeliveries'

export const getWebhookDeliveriesQueryKey = ({ path, query }: Omit<GetWebhookDeliveriesOptions, 'headers'>) => [{ url: '/api/v1/webhooks/subscriptions/:subscriptionId/deliveries', params: path }, ...(query ? [query] : [])] as const

type GetWebhookDeliveriesQueryKey = ReturnType<typeof getWebhookDeliveriesQueryKey>

export function getWebhookDeliveriesQueryOptions({ path, query }: GetWebhookDeliveriesOptions, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = getWebhookDeliveriesQueryKey({ path, query })
  return queryOptions<GetWebhookDeliveriesStatus200, ResponseErrorConfig<Error>, GetWebhookDeliveriesStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await getWebhookDeliveries({ ...config, path, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
