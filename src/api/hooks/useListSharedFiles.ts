/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { ListSharedFilesOptions, ListSharedFilesStatus200 } from '../types/ListSharedFiles'
import { queryOptions } from '@tanstack/react-query'
import { listSharedFiles } from '../clients/listSharedFiles'

export const listSharedFilesQueryKey = ({ query }: Omit<ListSharedFilesOptions, 'headers'> = {}) => [{ url: '/api/v1/files/shared' }, ...(query ? [query] : [])] as const

type ListSharedFilesQueryKey = ReturnType<typeof listSharedFilesQueryKey>

export function listSharedFilesQueryOptions({ query }: ListSharedFilesOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listSharedFilesQueryKey({ query })
  return queryOptions<ListSharedFilesStatus200, ResponseErrorConfig<Error>, ListSharedFilesStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await listSharedFiles({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
