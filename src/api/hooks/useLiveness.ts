/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { LivenessStatus200 } from '../types/Liveness'
import { queryOptions } from '@tanstack/react-query'
import { liveness } from '../clients/liveness'

export const livenessQueryKey = () => [{ url: '/health/live' }] as const

type LivenessQueryKey = ReturnType<typeof livenessQueryKey>

export function livenessQueryOptions(config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = livenessQueryKey()
  return queryOptions<LivenessStatus200, ResponseErrorConfig<Error>, LivenessStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await liveness({ ...config, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
