/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { ReadinessStatus200, ReadinessStatus503 } from '../types/Readiness'
import { queryOptions } from '@tanstack/react-query'
import { readiness } from '../clients/readiness'

export const readinessQueryKey = () => [{ url: '/health/ready' }] as const

type ReadinessQueryKey = ReturnType<typeof readinessQueryKey>

export function readinessQueryOptions(config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = readinessQueryKey()
  return queryOptions<ReadinessStatus200, ResponseErrorConfig<ReadinessStatus503>, ReadinessStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await readiness({ ...config, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
