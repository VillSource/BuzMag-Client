/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { ListTrashedProductsOptions, ListTrashedProductsStatus200 } from '../types/ListTrashedProducts'
import { queryOptions } from '@tanstack/react-query'
import { listTrashedProducts } from '../clients/listTrashedProducts'

export const listTrashedProductsQueryKey = ({ query }: Omit<ListTrashedProductsOptions, 'headers'> = {}) => [{ url: '/api/v1/catalog/products/trash' }, ...(query ? [query] : [])] as const

type ListTrashedProductsQueryKey = ReturnType<typeof listTrashedProductsQueryKey>

export function listTrashedProductsQueryOptions({ query }: ListTrashedProductsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listTrashedProductsQueryKey({ query })
  return queryOptions<ListTrashedProductsStatus200, ResponseErrorConfig<Error>, ListTrashedProductsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await listTrashedProducts({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}
