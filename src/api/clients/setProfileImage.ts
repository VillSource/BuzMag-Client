/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { SetProfileImageOptions, SetProfileImageResponses } from '../types/SetProfileImage'
import { client } from '../.kubb/client'

/**
 * @description Persists a durable image URL on the current user's profile. Typically called after the Files module's presigned-upload flow returns a publicUrl. Pass a null/empty body to clear.
 * @summary Set the authenticated user's avatar URL
 * {@link /api/v1/identity/profile/image}
 */
export function setProfileImage<ThrowOnError extends boolean = true>(options: Options<SetProfileImageOptions, ThrowOnError>): Promise<RequestResult<SetProfileImageResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'PUT', url: '/api/v1/identity/profile/image', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<SetProfileImageResponses, ThrowOnError>>
}
