/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { ResendConfirmationEmailOptions, ResendConfirmationEmailResponses } from '../types/ResendConfirmationEmail'
import { client } from '../.kubb/client'

/**
 * @description Re-sends the email-confirmation link to a user who has not confirmed their address yet.
 * @summary Resend a user's email confirmation (admin)
 * {@link /api/v1/identity/users/:id/resend-confirmation-email}
 */
export function resendConfirmationEmail<ThrowOnError extends boolean = true>(options: Options<ResendConfirmationEmailOptions, ThrowOnError>): Promise<RequestResult<ResendConfirmationEmailResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'POST', url: '/api/v1/identity/users/{id}/resend-confirmation-email', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<ResendConfirmationEmailResponses, ThrowOnError>>
}
