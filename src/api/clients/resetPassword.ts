/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { ResetPasswordOptions, ResetPasswordResponses } from '../types/ResetPassword'
import { client } from '../.kubb/client'

/**
 * @description Reset the user's password using the provided verification token.
 * @summary Reset password
 * {@link /api/v1/identity/reset-password}
 */
export function resetPassword<ThrowOnError extends boolean = true>(options: Options<ResetPasswordOptions, ThrowOnError>): Promise<RequestResult<ResetPasswordResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'POST', url: '/api/v1/identity/reset-password', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<ResetPasswordResponses, ThrowOnError>>
}
