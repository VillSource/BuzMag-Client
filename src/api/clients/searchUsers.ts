/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { SearchUsersOptions, SearchUsersResponses } from '../types/SearchUsers'
import { client } from '../.kubb/client'

/**
 * @description Search and filter users with server-side pagination, sorting, and filtering by status, email confirmation, and role.
 * @summary Search users with pagination
 * {@link /api/v1/identity/users/search}
 */
export function searchUsers<ThrowOnError extends boolean = true>(options: Options<SearchUsersOptions, ThrowOnError> = {}): Promise<RequestResult<SearchUsersResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'GET', url: '/api/v1/identity/users/search', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<SearchUsersResponses, ThrowOnError>>
}
