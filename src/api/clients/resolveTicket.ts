/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { ResolveTicketOptions, ResolveTicketResponses } from '../types/ResolveTicket'
import { client } from '../.kubb/client'

/**
 * @summary Mark a ticket as resolved
 * {@link /api/v1/tickets/:ticketId/resolve}
 */
export function resolveTicket<ThrowOnError extends boolean = true>(options: Options<ResolveTicketOptions, ThrowOnError>): Promise<RequestResult<ResolveTicketResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'POST', url: '/api/v1/tickets/{ticketId}/resolve', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<ResolveTicketResponses, ThrowOnError>>
}
