/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { GetPermissionCatalogOptions, GetPermissionCatalogResponses } from '../types/GetPermissionCatalog'
import { client } from '../.kubb/client'

/**
 * @description Returns every permission registered in the host, filtered to the caller's tenant context. Non-root tenants see the Admin set; the root tenant additionally sees the platform Root set.
 * @summary Get permission catalog
 * {@link /api/v1/identity/permissions/catalog}
 */
export function getPermissionCatalog<ThrowOnError extends boolean = true>(options: Options<GetPermissionCatalogOptions, ThrowOnError> = {}): Promise<RequestResult<GetPermissionCatalogResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'GET', url: '/api/v1/identity/permissions/catalog', security: [{ type: 'http', scheme: 'bearer' }], ...config }) as Promise<RequestResult<GetPermissionCatalogResponses, ThrowOnError>>
}
